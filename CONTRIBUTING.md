# 欢迎反馈与参与改进

谢谢你试用拾词！欢迎提出问题、想法和改进建议。我会根据反馈复现问题、修正错误，并逐步完善这个工具。

## 反馈问题

请先搜索已有 Issues。没有相同问题时，选择合适的模板：

- **问题反馈**：无法启动、划词不工作、卡片异常、计数或复习问题。
- **功能建议**：说明你在什么场景下需要这个功能，以及目前哪里不方便。
- **词典 / 词形纠错**：提供词语、当前结果、预期结果；如果有可靠资料链接，请一起附上。

尽量包含 Windows 版本、软件版本、目标程序、复现步骤和少量不敏感的示例文字。截图请脱敏，不要上传整个 `data` 文件夹、查询缓存、账号、密钥或私人文档。

## 提交代码

1. Fork 仓库并创建主题分支。
2. 按 README 准备词典并构建，保持改动范围清晰。
3. 业务逻辑变更请补充对应测试；界面变更请提供前后截图。
4. 提交 PR，说明解决的问题、最终行为、测试结果和已知限制。

源码保持 UTF-8 和 LF。不要提交生成的数据库、EXE、DLL、ZIP、测试临时数据或个人查询记录。

`source/Program.cs` 中包含轻量自测入口：

- `--self-test`：词形归并、第 4 次入本、持久化、缓存、复习间隔、导出与 Unicode 分段。
- `--self-test --network-test`：再验证两条真实联网翻译，会消耗少量免费额度。
- `--selection-test`：在隔离进程间验证可访问文本选区。
- `--interaction-test`：实际鼠标双击 → 浮钮 → 卡片 → 本地记录。需要可交互桌面，会暂时移动鼠标。
- `--render-test`：使用隔离演示数据生成页面预览。

本地自测数据写入可执行文件旁的 `.selftests`，可通过 `SHICI_TEST_DIR` 环境变量覆盖。报告写在测试用可执行文件旁；请不要对正在日常使用的实例同时运行交互自测。

新增词根资料请给出依据，区分词形还原、构词法和历史词源，不要仅凭字母相似推断。修改开源组件或词典数据时，保留来源与许可证。

## 发布

维护者运行 `scripts/build.ps1 -Package` 得到干净的发布包和 SHA-256。发布包包含程序、词典、文档和第三方许可证，不包含 `data`、个人导出文件、PDB、绝对路径快捷方式或调试报告。打包前请先完成测试。
